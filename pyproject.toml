[project]
name = "simple-land-registry-data-ingestion"
version = "0.1.0"
description = "Downloads the UK Land Registry Price Paid dataset and syncs it to a Postgres database"
requires-python = ">=3.11"
dependencies = [
    "pandas==2.3.3",
    "psycopg[binary]==3.2.12",
    "requests==2.32.5",
    "sqlalchemy==2.0.44",
    "typeguard==4.4.4",
]

[tool.uv]
package = false
